#pragma once

// Please *only* insert global definitions that are used in several places.

// Paths
#define MODEL_PATH "models" + Path::DirectorySeparatorChar
#define TEXTURE_PATH "textures" + Path::DirectorySeparatorChar
